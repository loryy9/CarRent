<nav class="navbar navbar-expand-lg shadow-sm mt-2 mt-lg-4">
    <div class="container-fluid">
        <a href="index.html">
            <img
                src="/assets/noBgColorGold.png"
                alt="Logo"
                height="30"
            />
        </a>
        <button
            class="navbar-toggler border-0"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="custom-toggle-icon">
                <img
                    src="/assets/menu-burger.png"
                    alt="hamburger menu"
                />
            </span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul
                class="navbar-nav text-center mx-md-auto pt-4 pt-lg-0 mx-lg-0 ms-lg-auto"
            >
                <li class="nav-item <%= typeof active !== 'undefined' && active === 'home' ? 'active' : '' %>">
                    <a class="nav-link" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item <%= typeof active !== 'undefined' && active === 'catalogo' ? 'active' : '' %>">
                    <a class="nav-link" href="/catalogo">Catalogo</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/#chi-siamo">Chi Siamo</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/#contatti">Contatti</a>
                </li>
            </ul>
            <div class="btn d-flex align-items-center justify-content-center gap-2 btn-outline-warning ms-lg-3 mt-4 mt-lg-0">
                <img src="/assets/login_white.png" alt="login-button" />
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/dashboard">Dashboard</a>
                <% } else { %>
                    <a href="/login">Accedi</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>